name: Keep Render Alive

on:
  schedule:
    - cron: '*/14 * * * *'   # every 14 minutes
  workflow_dispatch:           # manual trigger too

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: |
          curl -f https://clinic-booking-system-mhi3.onrender.com/api/doctors || echo "Ping failed"